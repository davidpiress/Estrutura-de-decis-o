"""
Definindo uma estrutura para saber se e "IMPAR" ou "PAR"
e em seguida calculando com o numero que a condição determinou se e impar ou par
e ao final de tudo atraves da soma gerando um resultado de impa ou par novamente
"""

while True:
    try:
        numero = int(input('Digite um numero: '))

        if numero % 2 == 0:
            print(f'O {numero} e par.')
        else:
            print(f'O {numero} e impar.')
    except ValueError:
        print('A letra não pode ser convertida para numero.')
        break
    print('Agora eu precisar que você possa somar o numero que e impar ou par '
        'para gerar outro impar ou par'
          )

    print()

    try:
        numero2 = input('Digite o numero para soma: ')
        numero2 = int(numero2)
        soma = numero + numero2

        if soma % 2 == 0:
            print(f'O resultado da soma e {soma} e é par')
            break
        else:
            print(f'O resultado da soma e {soma} e é impar')
            break
    except ValueError:
        print('A letra não pode ser convertida para numero.')
        break
